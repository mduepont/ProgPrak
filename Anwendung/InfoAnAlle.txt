
1.Unter DBTreiber befindet sich der Treiber, den wir für postgres verwenden.
2. Unter WebContent/WEB-INF/lib liegt die Bibliothekt für JSF.
Die Ordnerstruktur ist der aus Eclipse angepasst, bitte immer nur die Quellcode 
Dateien in den entsprechenden Ordner packen.
-------------------------------------------------------------
Zwischenstand I
-Datenbank Zugriff für den ersten Prototyp ist soweit fertig, Methoden:

!Ablauflogik fehlt noch, daher vorher Aufrufe noetig:
	Methoden der DAOImple Klasse:
		getProperties();
		verbinde();
		statementsVorbereiten();
		schliesseVerbindung(wdw.getVerbindung());!

	-public ArrayList<String> anlaesseLaden(); 
	-public int speichereErsteller(WunschlisteErsteller ersteller);
	-public WunschlisteErsteller ladeErsteller(String email);
	-public boolean loescheErsteller(int id);
	-public int speichereWunschliste(Wuenschliste liste);
	-public Wuenschliste ladeWunschliste(int id);