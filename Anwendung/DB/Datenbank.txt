     DROP TABLE IF EXISTS Wunschliste;
    DROP TABLE IF EXISTS Wunschlistenersteller;
    DROP TABLE IF EXISTS Desing;
    DROP TABLE IF EXISTS Wünsche;
    DROP TABLE IF EXISTS Geschenk;
    
   /* DROP DOMAIN IF EXISTS      ;  -> vorgegebene eingaben definieren
      CREATE DOMAIN ..... AS ....;   -> inhaltliche eingaben
   
   */
   CREATE TABLE Wunschliste(
       id_Wunschlistenersteller integer ,
       name_Wunschliste         text,
       id_Wunschliste           integer PRIMARY KEY ,
       anlass                   text,
       ablaufdatum              date,
       listenpassword           text,
       desing_ID                integer,
       uberraschungsmodus       boolean NOT NULL 
       /*FOREIGN KEY () REFERENCES ()   -> verbindung zwischen den tabellen herstellen*/
   );
   
  /* INSERT INTO Wunschliste (id_Wunschlistenersteller, name_Wunschliste,id_Wunschliste,anlass,ablaufdatum,listenpassword,desing_ID,uberraschungsmodus)
           VALUES
           ()
           */
      CREATE TABLE Wunschlistenersteller(
          name_Wunschlistenersteller text,
          e_Mail                     text,
          id_Wunschlistenersteller   integer PRIMARY KEY
          /*FOREIGN KEY ....REFERENCES ...(...)*/
      );
      
      /* INSERT INTO Wunschlistenersteller (name_Wunschlistenersteller,e_Mail,id_Wunschlistenersteller)
           VALUES
           ()
           */
      CREATE TABLE Desing(
          desing_ID        integer PRIMARY KEY,
          hintergrundfarbe text,
          schriftfarbe     text,
          schriftart       text
          /*FOREIGN KEY ....REFERENCES ...(...)*/
      );
      /* INSERT INTO Desing (desing_ID, hintergrundfarbe, schriftfarbe, schriftart)
           VALUES
           ()
           */
      CREATE TABLE Wünsche(
          name_Wunsch    text,
          id_Wunsch      integer PRIMARY KEY,
          id_Wunschliste integer,
          beschreibung   text,
          linkZuWunsch   text
          /*FOREIGN KEY ....REFERENCES ...(...)*/
      );
      /* INSERT INTO Wünsche (name_Wunsch, id_Wunsch,id_Wunschliste,beschreibung,linkZuWunsch)
           VALUES
           ()
           */
      CREATE TABLE Geschenk(
          id_Wunsch     integer PRIMARY KEY,
          name_Schenker text 
          /*FOREIGN KEY ....REFERENCES ...(...)*/
      );
      /* INSERT INTO Geschenk (id_Wunsch,name_Schenker)
           VALUES
           ()
           */
         
       commit;